kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: temenos-dev-pipeline-claim
  namespace: temenos-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:

